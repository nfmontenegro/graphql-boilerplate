type Query {
  user(id: String!): User!
}

type Mutation {
  post(name: String!, lastname: String!, email: String!, password: String!): User
}

type User {
  id: ID!
  name: String!
  lastname: String!
  email: String!
  password: String!
}